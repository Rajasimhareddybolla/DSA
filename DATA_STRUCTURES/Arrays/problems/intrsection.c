#include <stdlib.h>
int* intersection(int* nums1, int nums1Size, int* nums2, int nums2Size, int* returnSize) {
    if (nums1Size>nums2Size){
       
    }
    else{
        int *answers = malloc(sizeof(int)*nums1Size);
    }
    int counter = 0;
     for(int i = 0 ; i < nums1Size;i++){
        for(int k = 0 ;k<nums2Size;k++ ){
            if(nums1[i] == nums2[k]){
                *(answers+counter)
            }
        }
        }
}