package stack_quee;

import stack_quee.quees.quee_dtype;

public class valid {
    public boolean isValid(String s) {
        quee_dtype no = new quee_dtype();
        
        for (int i = 0 ; i < s.length() ; i++){
            if (i % 2 == 0 ){
                
            }
        }
        return false;
    }
}
